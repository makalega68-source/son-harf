# v0.8.2 QA fixes

- Adds a real **Beni hatırla** option on the sign-in screen.
- When enabled, the remembered e-mail is prefilled and the authenticated session is kept across app launches.
- When disabled, the saved session is cleared on the next app launch.
- Makes the match-summary overlay reliably dismissible.
- Persists the dismissed finished-room id so the polling loop cannot immediately reopen the same summary.
- Enlarges the close target and adds an explicit **Özeti kapat ve devam et** action.
