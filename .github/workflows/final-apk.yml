name: Deprecated Final APK Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "This workflow is retired. Use Android CI or Current APK Build instead."
