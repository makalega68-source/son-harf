name: Siege Readability V35

on:
  push:
    branches:
      - claude/kelime-tahti-home-redesign-7ojb7d
    paths:
      - 'tools/polish_siege_readability_v35.py'
      - 'tools/fix_v35_contract_escape.py'
      - '.github/workflows/siege-readability-v35.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  readability-test-build:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Checkout canonical branch
        uses: actions/checkout@v4
        with:
          ref: claude/kelime-tahti-home-redesign-7ojb7d
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK 36
        shell: bash
        run: |
          SDKMANAGER="${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager"
          "$SDKMANAGER" --sdk_root="$ANDROID_HOME" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          validate-wrappers: false

      - name: Apply readability polish
        shell: bash
        run: |
          python3 tools/polish_siege_readability_v35.py
          python3 tools/fix_v35_contract_escape.py

      - name: Unit tests and debug APK
        run: ./gradlew clean :app:testDebugUnitTest :app:assembleDebug --stacktrace --warning-mode all

      - name: Commit verified source
        shell: bash
        run: |
          git config user.name "kelime-tahti-ci"
          git config user.email "actions@github.com"
          git add app/build.gradle.kts \
                  app/src/main/java/com/sonharf/game/WordSiegeCompactBag.kt \
                  app/src/main/java/com/sonharf/game/WordSiegePracticeScreen.kt \
                  app/src/main/java/com/sonharf/game/WordSiegePracticeBoard.kt \
                  app/src/main/java/com/sonharf/game/WordSiegePanMatch.kt \
                  app/src/test/java/com/sonharf/game
          git rm tools/polish_siege_readability_v35.py tools/fix_v35_contract_escape.py
          git diff --cached --check
          git commit -m "Polish siege bag, chat and bonus readability"
          git push origin HEAD:claude/kelime-tahti-home-redesign-7ojb7d

      - name: Prepare APK
        shell: bash
        run: |
          mkdir -p final-apk
          cp app/build/outputs/apk/debug/app-debug.apk final-apk/Kelime-Tahti-Guncel-v35.apk
          sha256sum final-apk/Kelime-Tahti-Guncel-v35.apk > final-apk/SHA256.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: kelime-tahti-siege-v35
          path: final-apk/
          if-no-files-found: error
          retention-days: 7
