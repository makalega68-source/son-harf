name: Harf Yolu Blue Turquoise Refresh

on:
  push:
    branches:
      - codex/kelime-kusatmasi-premium-logo-20260918
    paths:
      - .github/workflows/harf-yolu-blue-turquoise-refresh.yml

permissions:
  contents: write

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout working branch
        uses: actions/checkout@v4
        with:
          ref: codex/kelime-kusatmasi-premium-logo-20260918
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android SDK 36
        shell: bash
        run: |
          SDKMANAGER="${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager"
          "$SDKMANAGER" --sdk_root="$ANDROID_HOME" "platforms;android-36" "build-tools;36.0.0"

      - name: Apply Harf Yolu blue turquoise refresh
        run: python3 .github/scripts/patch_harf_yolu_blue_turquoise.py

      - name: Run Harf Yolu regression tests
        run: ./gradlew testDebugUnitTest --tests com.sonharf.game.LetterLadderUxRegressionTest --tests com.sonharf.game.LetterLadderEngineTest

      - name: Build debug APK
        run: ./gradlew assembleDebug

      - name: Commit verified product changes and remove temporary automation
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/sonharf/game/LetterLadderGame.kt
          git rm .github/scripts/patch_harf_yolu_blue_turquoise.py
          git rm .github/workflows/harf-yolu-blue-turquoise-refresh.yml
          git commit -m "Harf Yolu arayuzunu mavi turkuaz dinamik yap"
          git push origin HEAD:codex/kelime-kusatmasi-premium-logo-20260918

      - name: Stage APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Kelime-Kusatmasi-Harf-Yolu-Mavi-Turkuaz.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Kelime-Kusatmasi-Harf-Yolu-Mavi-Turkuaz-APK
          path: Kelime-Kusatmasi-Harf-Yolu-Mavi-Turkuaz.apk
          if-no-files-found: error
